spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update # 매번 테이블을 삭제 후 재생성
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL8 방언 사용